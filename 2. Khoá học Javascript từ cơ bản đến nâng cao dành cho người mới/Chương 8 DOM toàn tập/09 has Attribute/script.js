// selector.hasAttribute("attribute") -> kiểm tra selector có attribute nào đó hay không, nếu có -> true ngược không trả về false

const p = document.querySelector("spinner");

if (p.hasAttribute("class")) {
    // do something here
}
