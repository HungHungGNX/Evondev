const listNumberTripple = listNumber.forEach((value, index, array) => {
    console.log(value);
});

// Hãy cho biết sự khác nhau giữa foreach và map
// - foreach không return được còn map thì return được
// - map từ mảng cũ sẽ tạo ra 1 mảng mới
// - Không có cách nào dừng foreach được
